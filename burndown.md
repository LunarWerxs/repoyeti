# RepoYeti burndown — design-alignment & consolidation hitlist

> **STATUS 2026-07-04: COMPLETE.** Every item below (A1–G17, including this repo's part
> of every cross-repo item) is implemented, verified, and committed. Shared pieces
> landed in the lunarwerx-ui kit (lib/: relativeTime, httpClient, useSelfUpdate,
> i18n-core, theme; server-lib/: mcp-stdio, instance-pointer, updater-engine,
> find-free-port) and were adopted here. Do not re-work items from this list.

Generated 2026-07-03 by a cross-repo audit of the three sibling apps (RepoYeti, DevWebUI, Reimagine) against the shared `lunarwerx-ui` design kit; every item below was independently re-verified against the working tree. Work items top-to-bottom within each section; quick wins (S effort) are sorted first, then M, then L.

## How to work this list (guardrails)

- Never edit `web/src/components/ui/**`, `web/src/shell/**`, `web/src/lib/utils.ts`, or `web/src/styles/kit-*.css` directly — those are synced from `D:/PublicProjects/lunarwerx-ui`. Fix in the kit, then run `node sync.mjs` there.
- Never bump package versions, cut releases, or tag anything unless an item explicitly says to align a version.
- `src/config.ts` `BUILTIN_GROQ_KEY` is a deliberately-public throwaway key — never "fix" or scrub it.
- `web/src/components/FileViewer.vue` is intentionally bespoke (resizable Monaco) — leave it alone.
- RepoYeti's working tree may hold an **uncommitted account-switcher WIP** (`web/src/components/AccountSwitcher.vue`, `web/src/components/AppHeader.vue`, and related store/api/settings/db files). Several items below (notably **A1**) edit those same files. Before starting, run `git status`; if those files show as modified, do NOT revert, reset, `git checkout`, or stash them — edit around the existing uncommitted changes and leave them in place.
- After each item: run the verify commands — `bun run lint`, `bun run typecheck`, `bun run build` from the repo root for server-side changes; `cd web && bun run build` (runs i18n check + vue-tsc + vite build) for web changes; server tests via `bun test tests/` from the repo root; web tests via `cd web && bun run test` (vitest). Never claim done without them passing. Do not run plain `bun test` from the repo root — it reports false failures.
- Items marked [CROSS-REPO] also appear in the sibling `burndown.md` files (DevWebUI, Reimagine) — do only THIS repo's part here; the shared/canonical piece usually lands in the `lunarwerx-ui` kit.

## A. Design alignment (deviations from the Mira kit)

### A1. Hardcoded Tailwind palette colors (violet/emerald/amber/red) scattered across app code instead of semantic tokens — effort: M
- **Files:**
  - `web/src/components/settings/AiProvidersSection.vue`
  - `web/src/components/SmartCommitPlan.vue`
  - `web/src/components/repo-card/RepoCardHeader.vue`
  - `web/src/components/repo-card/RepoCardChanges.vue`
  - `web/src/components/smart-commit-plan/CommitCard.vue`
  - `web/src/components/AppHeader.vue`
  - `web/src/components/AccountSwitcher.vue`
  - `web/src/components/RepoList.vue`
- **Problem:** 13 occurrences of hardcoded Tailwind palette classes across 8 files instead of semantic tokens. Most notably `AiProvidersSection.vue:196-198` puts a hardcoded-violet "default provider" Badge (`class="border-violet-500/30 bg-violet-500/15 px-1.5 py-0 text-[10px] font-medium text-violet-700 dark:text-violet-300"`) directly next to a properly-semantic `variant="success"` Badge on line 190-192 — proving the kit Badge already supports semantic variants and this one just didn't use them. `SmartCommitPlan.vue:280,290` hardcodes `text-violet-300` on the Sparkles icon and Loader2 spinner for the AI-commit dialog. Violet is Reimagine's accent color — using it in RepoYeti bleeds a sibling app's brand color into this one. Other spots: `RepoCardHeader.vue:160` `bg-amber-400/15 text-amber-400` (starred badge), `:168` `bg-emerald-400/15 text-emerald-400` (auto-commit badge); `RepoCardChanges.vue:406` `text-amber-500/80` (truncation notice); `CommitCard.vue:101` `text-violet-300` (regenerate button); `AppHeader.vue:258,262,367` `bg-emerald-500`/`bg-red-500` (connection status dot); `AccountSwitcher.vue:77,89` `border-emerald-500/40 bg-emerald-500/5|10 text-emerald-600 dark:text-emerald-400` (active account); `RepoList.vue:92` `text-amber-400` (starred star icon).
- **Fix:** No new tokens/variants need to be added — they already exist (verified: `web/src/styles/kit-base.css:45-47,87-89,149-151` already defines `--success`/`--warning`/`--info` tokens, and `web/src/components/ui/badge/index.ts:10-17` already has `success`/`warning`/`info`/`primary` badgeVariants entries). `AiProvidersSection.vue:196-198` — change to `variant="primary"` (or plain `variant="default"`) using the existing Badge variant system, deleting the hardcoded violet classes. `SmartCommitPlan.vue:280,290` and `CommitCard.vue:101` — replace `text-violet-300` with `text-primary`. `RepoCardHeader.vue:160` (starred) — replace `bg-amber-400/15 text-amber-400` with `bg-warning/15 text-warning`. `RepoCardHeader.vue:168` (autoCommit) and `RepoList.vue:92` — replace `bg-emerald-400/15 text-emerald-400` / `text-amber-400` with `bg-success/15 text-success` (autoCommit) and `text-warning` (starred star icon) respectively, matching the pinned badge's existing `bg-primary/15 text-primary` pattern on the line directly above. `RepoCardChanges.vue:406` — `text-amber-500/80` to `text-warning/80`. `AppHeader.vue:258,262,367` and `AccountSwitcher.vue:77,89` (connection/active-account indicators) — replace emerald with `bg-success`/`text-success` and red with `bg-destructive`/`text-destructive`, both tokens already present.
- **Done when:** `grep -rnE "violet-|amber-[0-9]|emerald-|red-500" web/src/components` (excluding `components/ui/**`) returns zero hits and the web build/typecheck pass; visual: badges/dots render in the same relative hue via semantic tokens instead of raw palette classes.

## B. Bespoke code → kit components

### B1. App.vue renders raw vue-sonner Toaster instead of the kit's Sonner.vue wrapper — effort: S
- **Files:**
  - `web/src/App.vue`
  - `D:/PublicProjects/lunarwerx-ui/src/components/ui/sonner/Sonner.vue`
  - `D:/PublicProjects/lunarwerx-ui/kit.config.json`
- **Problem:** `App.vue:3,17-24` does `import { Toaster } from "vue-sonner";` and renders `<Toaster :theme="toasterTheme" position="bottom-center" :duration="3500" :offset="16" rich-colors close-button />` directly, bypassing the kit's `components/ui/sonner/Sonner.vue` wrapper. That kit wrapper (already synced into Reimagine) wires the toaster's CSS variables to the app's own semantic tokens (`--popover`, `--popover-foreground`, `--border`, `--radius`) and swaps in lucide icons (CircleCheckIcon, InfoIcon, TriangleAlertIcon, OctagonXIcon, Loader2Icon, XIcon) for the toast icons instead of vue-sonner's defaults. RepoYeti's `sonner` family is absent from its entry in `lunarwerx-ui/kit.config.json` (families list only has alert, badge, button, card, collapsible, dialog, dropdown-menu, input, label, popover, select, separator, sheet, switch, textarea, tooltip), so `components/ui/sonner/` was never synced into this app, and `rich-colors` on the raw Toaster uses vue-sonner's own built-in palette rather than the app's theme tokens.
- **Fix:** Add `"sonner"` to the `repoyeti.families` array in `D:/PublicProjects/lunarwerx-ui/kit.config.json`, run `node sync.mjs` from `D:/PublicProjects/lunarwerx-ui` to copy `components/ui/sonner/` into `web/src/components/ui/sonner/`, then in `App.vue` replace `import { Toaster } from "vue-sonner"` with `import { Toaster } from "@/components/ui/sonner"` (or the sonner index barrel) and drop the `rich-colors` prop (the kit wrapper's CSS-variable theming supersedes it) while keeping `:theme`, `position`, `:duration`, `:offset`, `close-button`. Note: there is no `web/kit.config.json` — the only kit.config.json to edit is in `lunarwerx-ui`.
- **Done when:** `web/src/components/ui/sonner/` exists, `App.vue` imports `Toaster` from `@/components/ui/sonner` (no `vue-sonner` import remains in App.vue), `rich-colors` prop is gone, and web build passes; visual: toasts pick up theme border/background/radius tokens.

### B2. AddRepo.vue reimplements a segmented-tab control instead of using a kit toggle-group — effort: M
- **Files:** `web/src/components/AddRepo.vue`
- **Problem:** `AddRepo.vue:82-88` defines a local `seg(active)` helper returning cva-style Tailwind classes, then `:147-159` renders 4 raw `<button>` elements manually wired with `:aria-pressed` to build a 4-way mode switch (register/create/clone/lore) styled as a segmented control (`grid grid-cols-2 gap-1 rounded-lg bg-secondary p-1`). This is a bespoke reimplementation of a toggle-group / tab-like widget. The kit ships a `toggle-group` family (used by DevWebUI and Reimagine) that provides this exact single-select segmented-button pattern with built-in accessibility (roving tabindex, ARIA) via reka-ui, but `toggle-group` is not in RepoYeti's `families` list in `kit.config.json`.
- **Fix:** Add `"toggle-group"` and `"toggle"` to the `repoyeti.families` array in `D:/PublicProjects/lunarwerx-ui/kit.config.json`, run `node sync.mjs`, then rewrite `AddRepo.vue`'s mode switch using `<ToggleGroup type="single" v-model="mode">` with 4 `<ToggleGroupItem>` children (one per mode, each still rendering its lucide icon + label), removing the local `seg()` helper and the 4 raw `<button aria-pressed>` elements.
- **Done when:** `web/src/components/ui/toggle-group/` and `web/src/components/ui/toggle/` exist; `grep -n "seg(" web/src/components/AddRepo.vue` returns zero hits; the mode switch renders via `<ToggleGroup>`/`<ToggleGroupItem>`; web build and tests pass.

## E. Dead code

### E1. runAutoCommitOnce() in src/auto-commit.ts is exported and never invoked anywhere — effort: S
- **Files:** `src/auto-commit.ts`, `tests/auto-commit.test.ts`
- **Problem:** `src/auto-commit.ts:296-304` defines and exports `runAutoCommitOnce()`:
  ```
  export function runAutoCommitOnce(): Promise<{ done: AutoCommittedRepo[]; blocked: AutoCommitBlockedRepo[] }> {
    return tick();
  }
  ```
  Its docstring says it "backs a possible future 'run auto-commit now' action" — it was scaffolded for a feature never wired up. `grep -rn runAutoCommitOnce src tests web` returns exactly one hit total: the definition itself in `src/auto-commit.ts`. It is not imported by `tests/auto-commit.test.ts` (that file's actual import list only includes `clampAutoCommitInterval`, `normalizeDailyAt`, `msUntilDailyAt`, `isAutoCommitActionable`, `planToCommits`, and 3 constants) and is not called from any HTTP route, MCP tool, or other src file.
- **Fix:** Delete the unused export from `src/auto-commit.ts` (lines ~296-304). No changes needed in `tests/auto-commit.test.ts` — it does not import `runAutoCommitOnce`.
- **Done when:** `grep -rn runAutoCommitOnce src tests web` returns zero results and `bun test tests/` passes.

### E2. hasStat() in web/src/lib/diffstat.ts is exported but never imported anywhere — effort: S
- **Files:** `web/src/lib/diffstat.ts`, `web/src/components/DiffStat.vue`
- **Problem:** `web/src/lib/diffstat.ts:1-8` exports `hasStat`:
  ```
  export function hasStat(s?: DiffStat | null): s is DiffStat {
    return (
      !!s && (s.addedLines > 0 || s.removedLines > 0 || s.addedChars > 0 || s.removedChars > 0)
    );
  }
  ```
  A repo-wide grep for `hasStat` across `web/src` (`*.ts` and `*.vue`) returns exactly one hit: its own definition. `DiffStat.vue` (the component that renders diff counts) instead does its own `v-if="stat"` check inline and imports only `fmtCount` from the same module — it never adopted `hasStat`.
- **Fix:** Delete the unused `hasStat` export from `web/src/lib/diffstat.ts` (lines 4-8). If a non-zero-delta check is actually needed later (e.g. to skip rendering an all-zero DiffStat badge), reintroduce it at the point of use.
- **Done when:** `grep -rn hasStat web/src` returns zero results and `cd web && bun run test` passes.

## F. Config & docs drift (in-repo)

### F1. CONTRIBUTING.md documents a multi-locale i18n system that does not exist in code — effort: S
- **Files:** `CONTRIBUTING.md`, `web/src/i18n.ts`, `web/src/locales/en.json`
- **Problem:** `CONTRIBUTING.md` §"Adding a language" (lines 56-64) instructs contributors to "Add `{ code: "<code>", label: "<Native name>" }` to `SUPPORTED_LOCALES` in `web/src/i18n.ts`" and states "The switcher (Settings → Appearance), persistence, and lazy-loading pick it up automatically. Non-English locales currently shipped are machine-translated drafts." None of this exists: `web/src/i18n.ts` has no `SUPPORTED_LOCALES` export (its only locale-related lines are `import en from "./locales/en.json"` and `locale: "en"`), there is no locale switcher component anywhere in `web/src` (grepped for `SUPPORTED_LOCALES|localeSwitch|LocaleSwitcher` — zero matches), `web/src/components/settings/AppearanceSection.vue` has no locale/language UI, and `web/src/locales/` contains only `en.json` — no other locale files ship. `i18n.ts`'s own top-of-file comment says the opposite: "RepoYeti ships a single locale (English)... To reintroduce other languages later: add `locales/<code>.json`... and a small locale switcher back" (i.e. the switcher doesn't exist yet). `README.md` line 60 correctly states "only English ships today," so `CONTRIBUTING.md` contradicts both the actual code and its own sibling doc.
- **Fix:** In `CONTRIBUTING.md`, replace the "## Internationalisation" → "### Adding a language" section (lines 56-64) with accurate text matching `web/src/i18n.ts`'s comment: state that RepoYeti currently ships English only, that the `t()`/`vue-i18n` layer exists so locales *can* be added later, and that adding one requires (1) creating `web/src/locales/<code>.json` with full key parity, (2) building a locale switcher and wiring it into `createI18n({ messages })` in `web/src/i18n.ts` (does not exist yet — must be built from scratch, not just registered in a `SUPPORTED_LOCALES` array), and (3) running `bun run i18n:check` to confirm parity. Remove the false claims about an existing switcher, persistence, lazy-loading, and "currently shipped" machine-translated locales.
- **Done when:** `CONTRIBUTING.md` no longer mentions `SUPPORTED_LOCALES` or an existing switcher; the section matches `web/src/i18n.ts`'s own comment describing the single-locale, switcher-not-yet-built state.

### F2. web/UI_UNIFICATION.md states the unified content width is 1180px; the shipped token is 800px — effort: S
- **Files:** `web/UI_UNIFICATION.md`, `web/src/styles/kit-base.css`, `D:/PublicProjects/lunarwerx-ui/src/styles/base.css`
- **Problem:** `web/UI_UNIFICATION.md` line 20-21 says: "**Unified content width:** **1180px** via the `--container-max` token / `AppContainer` (was RepoYeti `max-w-3xl`, DevWebUI `max-w-7xl`)." But the actual synced token in both the kit source (`lunarwerx-ui/src/styles/base.css:24`) and RepoYeti's synced copy (`web/src/styles/kit-base.css:24`) is `--container-max: 800px; /* unified app content width */` — matching the locked spec of 800px, not 1180px. This is a stale/incorrect number in a doc dated 2026-07-02 that explicitly claims to document the current design-system state.
- **Fix:** In `web/UI_UNIFICATION.md`, change "**1180px**" to "**800px**" in the "Unified content width" bullet (line 20-21) to match the actual `--container-max: 800px` token shipped in `web/src/styles/kit-base.css` and the kit source `lunarwerx-ui/src/styles/base.css`.
- **Done when:** `grep -n 1180 web/UI_UNIFICATION.md` returns zero hits and the doc states 800px.

## G. Cross-repo consolidation [CROSS-REPO]

### G1. Reimagine's HTTP server has no SIGINT/SIGTERM graceful-shutdown handler, unlike RepoYeti and DevWebUI — effort: S
- **Files:** `D:/NEWProjects/active/Reimagine/src/server.js`, `src/cli/lifecycle.ts`, `D:/PublicProjects/DevWebUI/devwebui/server/src/index.ts`
- **Problem:** RepoYeti registers `process.on("SIGINT", shutdown)` and `process.on("SIGTERM", shutdown)` in `src/cli/lifecycle.ts` (lines 206-207), where `shutdown` (defined at line 138) does cleanup including clearing the instance pointer. DevWebUI registers the same pair via `for (const sig of ["SIGINT", "SIGTERM"] as const) process.on(sig, ...)` in `devwebui/server/src/index.ts` (lines 80-81), plus a `process.on("exit", cleanup)` fallback (line 61). Reimagine's `src/server.js` has NO `process.on("SIGINT"/"SIGTERM", ...)` anywhere — the only shutdown-adjacent code is an explicit `/api/shutdown` HTTP route calling `server.close(() => process.exit(0))` (around line 283). Pressing Ctrl-C on a running Reimagine server does an ungraceful default-Node exit — SSE clients aren't notified, in-flight run bookkeeping isn't flushed the way RepoYeti/DevWebUI clean up their instance pointer file on Ctrl-C.
- **Fix:** No RepoYeti-side code change — this item's fix lands entirely in Reimagine (`server.js`: add `function shutdown() { server.close(() => process.exit(0)); setTimeout(() => process.exit(0), 1500).unref(); } process.on('SIGINT', shutdown); process.on('SIGTERM', shutdown);`, reusing the close-with-timeout-fallback pattern already used by the `/api/shutdown` route around line 283).
- **Repos involved:** RepoYeti (reference implementation only), DevWebUI (reference implementation only), Reimagine (needs the fix).
- **This repo's part:** None — RepoYeti's `src/cli/lifecycle.ts` SIGINT/SIGTERM handling is already correct and serves as the reference pattern; no changes needed here.
- **Done when:** (RepoYeti has nothing to verify for this item beyond confirming `src/cli/lifecycle.ts:206-207` already registers both signal handlers, which it does.)

### G2. Running-instance pointer (runtime.json) module is copy-pasted between RepoYeti and DevWebUI — effort: S
- **Files:** `src/instance.ts`, `D:/PublicProjects/DevWebUI/devwebui/server/src/instance.ts`
- **Problem:** Both files are near-identical: same `InstanceInfo` shape (`port`, `url`, `pid`, `startedAt`), same `writeInstanceInfo`/`readInstanceInfo`/`clearInstanceInfo`/`findLiveInstance` functions, same doc comments almost word-for-word ("The daemon may bind/pick a different port than requested... it records the port it ACTUALLY bound in ~/.<app>/runtime.json"), same `/api/health` probe pattern with `AbortSignal.timeout`, same best-effort try/catch-swallow style, same 0600-mode intent (RepoYeti passes `{ mode: 0o600 }` to `writeFileSync`; DevWebUI calls a separate `chmodSync(RUNTIME_FILE, 0o600)` after write — cosmetic, not a design difference). Only the `CONFIG_DIR`/home path constant and the health-check body shape (`body.service === "repoyeti"` vs plain `body?.ok`) differ.
- **Fix:** Move this module into the `lunarwerx-ui` kit's shared server-lib (or a small new package) as `createInstancePointer({ homeDirName, serviceName? })` returning `{ writeInstanceInfo, readInstanceInfo, clearInstanceInfo, findLiveInstance, instanceFilePath }`. RepoYeti calls it with `homeDirName: ".repoyeti", serviceName: "repoyeti"`; DevWebUI with `homeDirName: ".devwebui"`. Delete both bespoke `instance.ts` bodies in favor of the shared factory; keep each app's thin `CONFIG_DIR` resolution (e.g. `REPOYETI_HOME` env override) as the only per-app code.
- **Repos involved:** RepoYeti, DevWebUI.
- **This repo's part:** Once the shared `createInstancePointer` factory exists in the kit/shared package, delete `src/instance.ts`'s bespoke body and replace it with a call to the factory using `homeDirName: ".repoyeti", serviceName: "repoyeti"`, keeping only RepoYeti's `CONFIG_DIR`/`REPOYETI_HOME` resolution as local code.
- **Done when:** `src/instance.ts` imports `createInstancePointer` instead of defining `writeInstanceInfo`/`readInstanceInfo`/`clearInstanceInfo`/`findLiveInstance` locally; `bun test tests/` passes; `bun run typecheck` passes.

### G3. "Time ago" relative-time formatting reimplemented independently in RepoYeti and DevWebUI with the same bucket logic — effort: S
- **Files:** `web/src/lib/util.ts` (lines 71-80), `D:/PublicProjects/DevWebUI/devwebui/web/src/lib/format.ts` (lines 17-32)
- **Problem:** RepoYeti's `fromNow(ts)` and DevWebUI's `formatAgo(nowMs, ts)`/`formatAgoCoarse(nowMs, ts)` implement the identical bucketing algorithm — under 60s → seconds, under 60m → minutes, under 24h → hours, else days — independently. RepoYeti: `if (s < 60) return t("time.secondsAgo", {n:s}); const m = Math.round(s/60); if (m<60) return t("time.minutesAgo",{n:m}); const h = Math.round(m/60); if (h<24) return t("time.hoursAgo",{n:h}); return t("time.daysAgo", {n: Math.round(h/24)});`. DevWebUI: `if (s<60) return \`${s}s ago\`; const m = Math.floor(s/60); if (m<60) return \`${m}m ago\`; const h = Math.floor(m/60); if (h<24) return \`${h}h ago\`; return \`${Math.floor(h/24)}d ago\`;`. RepoYeti's is i18n-localized (routes through `t()`), DevWebUI's is hardcoded English.
- **Fix:** Add `lunarwerx-ui/src/lib/relativeTime.ts` exporting `formatAgo(nowMs: number, ts: number, t?: (key: string, params: {n:number}) => string): string` — promote RepoYeti's bucketing thresholds (its `Math.round` is arguably more correct than DevWebUI's `Math.floor` for boundary display) but make the `t` translation function an optional injected param defaulting to plain English suffix formatting (`${n}s ago` etc.) when omitted, so DevWebUI can keep using it without i18n today and RepoYeti can pass its `t()`. Add `lib/relativeTime.ts` to `sync.mjs`'s `SHARED` array. This is a small (~10 line) function — low-effort/optional, only worth doing at the same time as another `lib/` sync pass rather than standalone.
- **Repos involved:** RepoYeti, DevWebUI.
- **This repo's part:** After `lib/relativeTime.ts` lands in the kit and is synced in, delete `fromNow` from `web/src/lib/util.ts` and re-export `formatAgo` bound to RepoYeti's `t()`.
- **Done when:** `web/src/lib/util.ts` no longer defines `fromNow` locally (imports the shared `formatAgo` instead); `cd web && bun run test` passes.

### G4. SSE/live-update client: RepoYeti uses vueuse's useEventSource with auto-reconnect; DevWebUI and Reimagine hand-roll raw EventSource with no reconnect policy — effort: S
- **Files:** `web/src/store/index.ts` (lines 362-390), `D:/PublicProjects/DevWebUI/devwebui/web/src/store.ts` (lines 248-267), `D:/NEWProjects/active/Reimagine/src/web/src/stores/control/runs.ts` (lines 73-93)
- **Problem:** RepoYeti's `store/index.ts` `connect()` uses `@vueuse/core`'s `useEventSource("/api/events", [...eventNames], { autoReconnect: { retries: -1, delay: 2500 } })` — declarative, typed event list, explicit infinite-retry reconnect policy, reactive `status`/`event`/`data` triple via `watch()`. DevWebUI's `store.ts` `connect()` instead hand-rolls `new EventSource('/api/stream')` with manual `onopen`/`onerror`/`addEventListener`, no explicit reconnect policy (relies solely on the browser's built-in EventSource auto-retry). Reimagine's `runs.ts` `subscribe(id)` similarly hand-rolls `new EventSource(eventsUrl(id))` with manual `onmessage`/error handling and manual `es.close()` bookkeeping.
- **Fix:** No shared kit module needed here (each app's SSE event schema is entirely different — no shared payload shape to unify). This item is about DevWebUI's and Reimagine's own store files switching to `useEventSource`; RepoYeti's implementation is already the reference pattern and needs no change.
- **Repos involved:** RepoYeti (reference implementation only), DevWebUI, Reimagine.
- **This repo's part:** None — `web/src/store/index.ts` already uses `useEventSource` with `autoReconnect: { retries: -1, delay: 2500 }`; nothing to change here.
- **Done when:** (RepoYeti has nothing to verify for this item beyond confirming `web/src/store/index.ts:362-390` already uses `useEventSource`, which it does.)

### G5. Settings-file write in DevWebUI is not atomic/mode-restricted, unlike RepoYeti's config.json save — effort: S
- **Files:** `D:/PublicProjects/DevWebUI/devwebui/server/src/runtime.ts`, `src/config.ts`
- **Problem:** RepoYeti's `saveConfig()` (`src/config.ts` lines 533-549) writes to a `${CONFIG_PATH}.${pid}.${Date.now()}.tmp` file with `{ mode: 0o600 }`, then `renameSync`s it into place — atomic and owner-only, with catch-and-cleanup on failure. DevWebUI's `writeSettings()` (`devwebui/server/src/runtime.ts` lines 144-168) instead does a direct `writeFileSync(SETTINGS_FILE, JSON.stringify(next, null, 2))` with no mode argument and no temp-file+rename step — a crash or concurrent write mid-write can truncate/corrupt `~/.devwebui/settings.json`.
- **Fix:** This item's fix lands entirely in DevWebUI (`server/src/runtime.ts`: change `writeSettings()` to write to a temp path with `writeFileSync(tmp, ..., { mode: 0o600 })` then `renameSync(tmp, SETTINGS_FILE)`, wrapped in try/catch that removes the temp file on failure — mirroring RepoYeti's `saveConfig()` lines 533-549 as the template).
- **Repos involved:** RepoYeti (reference implementation only), DevWebUI (needs the fix).
- **This repo's part:** None — `src/config.ts`'s `saveConfig()` is already the correct reference pattern; no changes needed here.
- **Done when:** (RepoYeti has nothing to verify for this item beyond confirming `src/config.ts:533-549` already does the atomic temp+rename+0600 write, which it does.)

### G6. Reimagine has no biome.json or root tsconfig.json — server-side JS gets no linting or type-checking, unlike RepoYeti/DevWebUI — effort: S
- **Files:** `biome.json`, `tsconfig.json`, `D:/PublicProjects/DevWebUI/devwebui/biome.json`, `D:/NEWProjects/active/Reimagine/src/web/tsconfig.json`
- **Problem:** RepoYeti has `biome.json` and `tsconfig.json` at the repo root; DevWebUI has `devwebui/biome.json` and `devwebui/server/tsconfig.json`. Reimagine has NEITHER a root `biome.json` nor a root `tsconfig.json` anywhere — the only `tsconfig*.json` files that exist are under `src/web/` (the Vue frontend's own Vite-generated configs). Reimagine's entire server tier (plain JS) has zero lint coverage and no type checking.
- **Fix:** This item's fix lands entirely in Reimagine (add a root `biome.json` modeled on RepoYeti's, scoping `files.includes` to `["**/*.js", "**/*.mjs", "!src/web/**"]`, enabling the linter's `recommended` preset, adding a `"lint": "biome lint ."` script; optionally a root `jsconfig.json`/`tsconfig.json` with `"allowJs": true, "checkJs": true, "noEmit": true` for basic type-checking, with a `"typecheck"` script if adopted).
- **Repos involved:** RepoYeti (reference implementation only), DevWebUI (reference implementation only), Reimagine (needs the fix).
- **This repo's part:** None — RepoYeti's root `biome.json`/`tsconfig.json` already exist and serve as the reference; no changes needed here.
- **Done when:** (RepoYeti has nothing to verify for this item beyond confirming its own `biome.json`/`tsconfig.json` already exist at root, which they do.)

### G7. biome.json scope/formatter settings diverge between RepoYeti and DevWebUI beyond what the differing stacks justify — effort: S
- **Files:** `biome.json`, `D:/PublicProjects/DevWebUI/devwebui/biome.json`
- **Problem:** RepoYeti's `biome.json` disables the formatter entirely (`"formatter": { "enabled": false }`) and only lints `**/*.ts` + `**/*.mjs`, with no `overrides` array at all. DevWebUI's `biome.json` enables the formatter (double quotes, 2-space indent, 100 col width), lints everything except a small ignore list, and has an `overrides` array with a targeted linter override disabling `noUnusedImports`/`noUnusedVariables` for `**/*.vue` plus a full linter-disable for `web/src/components/ui/**` (the kit-synced folder — correctly exempted per the kit-sync policy). RepoYeti's `biome.json` has no equivalent override, meaning kit-synced files in RepoYeti's web app ARE linted by RepoYeti's own config — and separately, RepoYeti's `.vue` files aren't caught by lint or format at all since `files.includes` is only `**/*.ts`/`**/*.mjs`.
- **Fix:** In `biome.json`, add an `overrides` array with the same `{ "includes": ["web/src/components/ui/**"], "linter": { "enabled": false } }` block DevWebUI already has, so kit-synced components are never linted/flagged in RepoYeti either. Separately decide whether RepoYeti's `web/*.vue` files should be included in its biome `files.includes` list (currently not lint-checked by biome at all, unlike DevWebUI) — if intentional (e.g. a separate eslint-vue setup covers them), leave a comment in `biome.json` noting why; otherwise add `"web/**/*.vue"` to the includes list to match DevWebUI's coverage.
- **Repos involved:** RepoYeti, DevWebUI.
- **This repo's part:** Add the `overrides` block exempting `web/src/components/ui/**` from linting to RepoYeti's `biome.json`, and decide/document the `.vue` file lint-coverage gap.
- **Done when:** `biome.json` has an `overrides` entry disabling the linter for `web/src/components/ui/**`; `bun run lint` passes with no new findings inside that folder.

### G8. Port-selection/bind-retry logic reimplemented three different ways with different guarantees — effort: M
- **Files:** `src/cli/lifecycle.ts`, `D:/PublicProjects/DevWebUI/devwebui/server/src/ports.ts`, `D:/NEWProjects/active/Reimagine/src/server.js`
- **Problem:** DevWebUI's `findFreePort()` (`server/src/ports.ts` lines 97-133) binds a throwaway `net.Server` at each candidate port and walks upward on `EADDRINUSE`/`EACCES` — a race-free probe. RepoYeti's `listen()` (`src/cli/lifecycle.ts` lines 250-265) does the same walk-upward-on-bind-failure idea but inline/bespoke using `Bun.serve` directly in a for-loop (20 attempts, hardcoded), duplicating the algorithm without sharing DevWebUI's `ports.ts` module. Reimagine's `server.js` (lines 418-434) takes a materially different strategy: on `EADDRINUSE` it retries the SAME port up to 25 times with a fixed 400ms delay (`LISTEN_RETRY_MS`), never hopping to a new port, and only gives up (`process.exit(1)`) if that port never frees.
- **Fix:** Factor RepoYeti's inline `listen()` to call a shared `findFreePort`-style helper (either import DevWebUI's `ports.ts` logic into a shared kit module, or have RepoYeti depend on the same small helper) instead of re-deriving the bind-and-walk loop with `Bun.serve`. (Reimagine's divergent same-port-retry strategy is a separate decision for that repo — leave a comment there explaining the intentional divergence, or add hop-to-free-port as a fallback after retries are exhausted.)
- **Repos involved:** RepoYeti, DevWebUI, Reimagine.
- **This repo's part:** Replace `src/cli/lifecycle.ts`'s inline 20-attempt `Bun.serve` for-loop with a call to a shared `findFreePort`-style helper (extracted from or shared with DevWebUI's `ports.ts`), instead of re-deriving the same bind-and-walk algorithm locally.
- **Done when:** `src/cli/lifecycle.ts`'s `listen()` calls a shared/imported port-finding helper rather than containing its own bind-loop; `bun test tests/` passes; manually confirm the daemon still hops to the next free port when the default port is occupied.

### G9. Self-update (git check/apply) logic is duplicated near-verbatim in all three apps — effort: M
- **Files:** `src/updater.ts`, `D:/PublicProjects/DevWebUI/devwebui/server/src/updater.ts`, `D:/NEWProjects/active/Reimagine/src/updater.js`
- **Problem:** All three files implement the exact same algorithm with the same function names, same regexes, and mostly identical comments: `packageVersion()`, `runCommand()` (spawns with a timeout and `GIT_TERMINAL_PROMPT=0`), `git()`/`gitText()`, `parseRemoteHead()`/`parseLsRemoteCommit()` (identical regexes `/^ref:\s+refs\/heads\/(.+)\s+HEAD$/m` and `/^([0-9a-f]{40})\s+/m`), `currentUpstream()`/`remoteForCheck()` (only the env var name differs: `REPOYETI_UPDATE_REPO`/`DEVWEBUI_UPDATE_REPO`/`REIMAGINE_UPDATE_REPO`), `checkForUpdate()` (same fallback/reason strings word-for-word), `commandSummary()`/`runStep()`/`applyUpdate()` (line-for-line identical control flow, differing only in the install/build command and identical timeout constants `CHECK_TIMEOUT_MS=30_000`, `APPLY_TIMEOUT_MS=120_000`, `BUILD_TIMEOUT_MS=240_000`).
- **Fix:** Extract a single generic updater module (published from the `lunarwerx-ui` kit repo, or a new tiny shared npm package `@lunarwerx/git-updater`) exporting `checkForUpdate(opts)` / `applyUpdate(opts)` where `opts = { appRoot, serviceName, updateRepoEnvVar, installCmd: string[], buildCmd: string[] }`. RepoYeti and DevWebUI (both Bun+TS) can share the exact `runCommand` (`Bun.spawn`) implementation; Reimagine (plain Node) needs a `node:child_process` spawn variant implementing the same interface.
- **Repos involved:** RepoYeti, DevWebUI, Reimagine.
- **This repo's part:** Once the shared updater module exists, delete `src/updater.ts`'s bespoke `parseRemoteHead`/`parseLsRemoteCommit`/`currentUpstream`/`remoteForCheck`/`checkForUpdate`/`commandSummary`/`runStep`/`applyUpdate` bodies and call the shared module with `{ appRoot: <repoyeti root>, serviceName: "repoyeti", updateRepoEnvVar: "REPOYETI_UPDATE_REPO", installCmd: ["bun", "install"], buildCmd: ["bun", "run", "--cwd", "web", "build"] }`.
- **Done when:** `src/updater.ts` imports the shared `checkForUpdate`/`applyUpdate` instead of defining them locally; `bun test tests/` passes; manual check that `/api/updates` and `/api/updates/apply` behave identically to before.

### G10. Fetch client wrapper (ApiError class + non-2xx handling) reimplemented 3x with near-identical shape — effort: M
- **Files:** `web/src/api.ts` (lines 30-54), `D:/PublicProjects/DevWebUI/devwebui/web/src/api.ts` (lines 40-70), `D:/NEWProjects/active/Reimagine/src/web/src/lib/api.ts` (lines 19-42)
- **Problem:** Every app defines its own `ApiError extends Error` (status + optional code/body) and its own private `req`/`request` helper that fetches, checks `!res.ok`, best-effort parses a JSON error body for a message/code, throws `ApiError`, else parses and returns JSON. RepoYeti's version (`api.ts` lines 30-54) has the most complete error-body extraction (`data.code ?? "ERROR"`, `data.message ?? data.error ?? res.statusText`) plus `credentials: "include"` and `AbortSignal` support threaded through `req<T>(method, path, body?, signal?)`. DevWebUI splits `req`/`reqJson`; Reimagine's `request<T>` extracts only `body.error` with a generic `'Request failed (status)'` fallback.
- **Fix:** Add `lunarwerx-ui/src/lib/httpClient.ts` as the canonical shared primitive, promoting RepoYeti's version as the base. Export `ApiError extends Error { status: number; code?: string; body?: unknown; ... }` and `httpRequest<T>(method: string, path: string, opts?: { body?: unknown; signal?: AbortSignal; credentials?: RequestCredentials }): Promise<T>`, reading `code`/`message`/`error` from the JSON body defensively (union of all three apps' current field names) so it's a strict superset. Add `lib/httpClient.ts` to the `SHARED` array in `lunarwerx-ui/sync.mjs` (line 39). DevWebUI's `credentials: "include"` behavior should become an optional param since DevWebUI/Reimagine don't currently pass it.
- **Repos involved:** RepoYeti, DevWebUI, Reimagine.
- **This repo's part:** Once `lib/httpClient.ts` is synced in, delete RepoYeti's local `ApiError` class and `req` function from `web/src/api.ts`, import `httpRequest`/`ApiError` from the synced module instead, and keep only the per-endpoint `export const api = {...}` method table calling `httpRequest<T>(...)`.
- **Done when:** `web/src/api.ts` no longer defines `ApiError`/`req` locally (imports them from the synced kit module); `cd web && bun run test` and `cd web && bun run build` pass.

### G11. Self-update check/apply (types + api methods + store state machine + toast UI) triplicated verbatim — effort: M
- **Files:** `web/src/types.ts` (lines 459-481), `web/src/api.ts` (lines 174-177), `web/src/store/index.ts` (lines 301-323), `web/src/components/AppHeader.vue` (lines 93-116), `D:/PublicProjects/DevWebUI/devwebui/shared/dto.ts` (lines 220-242), `D:/PublicProjects/DevWebUI/devwebui/web/src/api.ts` (lines 116-120), `D:/PublicProjects/DevWebUI/devwebui/web/src/store.ts` (lines 210-230), `D:/PublicProjects/DevWebUI/devwebui/web/src/components/TopBar.vue` (lines 123-145), `D:/NEWProjects/active/Reimagine/src/web/src/types/index.ts` (lines 206-228), `D:/NEWProjects/active/Reimagine/src/web/src/lib/api.ts` (lines 69-70), `D:/NEWProjects/active/Reimagine/src/web/src/stores/control/app-lifecycle.ts` (lines 39-65), `D:/NEWProjects/active/Reimagine/src/web/src/components/app/AppActionsBar.vue` (lines 83-101)
- **Problem:** All three apps independently implement an identical self-update feature end-to-end. `UpdateStatus`/`UpdateApplyResult` interfaces are byte-identical except the `service` string literal. The api client methods are identical one-liners (`checkUpdate`/`applyUpdate`). The store-side state machine is copy-pasted (`checkForUpdate()`/`applyUpdate()` with the same guard/try/catch/finally shape and `updateChecking`/`updateApplying` refs). The UI click handler is also copy-pasted 3x in `AppHeader.vue`/`TopBar.vue`/`AppActionsBar.vue` (`if (!status?.ok) {...} else if (!status?.updateAvailable) {...} else if (!status.canApply) {...} else {...}` toast branching), with Reimagine's version using hardcoded English strings instead of i18n keys.
- **Fix:** Create `lunarwerx-ui/src/lib/useSelfUpdate.ts` as the canonical shared composable, promoting RepoYeti's version as the base (cleanest guard logic) but genericizing the `service` field and dropping RepoYeti-specific i18n key names in favor of parameterized callback options. Shape: `useSelfUpdate(api: { checkUpdate(): Promise<UpdateStatus>; applyUpdate(): Promise<UpdateApplyResult> })` returning `{ updateStatus, updateChecking, updateApplying, checkForUpdate, applyUpdate }`. Also add a shared `handleUpdateClick(store, t)` helper encapsulating the toast branching so each header component becomes a 1-line call. Add `lib/useSelfUpdate.ts` to the `SHARED` array in `lunarwerx-ui/sync.mjs` (line 39, alongside `lib/utils.ts`). The `UpdateStatus`/`UpdateApplyResult` interfaces stay per-app (they're DTOs mirroring each app's own server), documented as intentionally-identical scaffolding rather than each redefined ad hoc.
- **Repos involved:** RepoYeti, DevWebUI, Reimagine.
- **This repo's part:** Once `lib/useSelfUpdate.ts` and the toast-branch helper are synced in, delete `web/src/store/index.ts`'s local `checkForUpdate`/`applyUpdate`/`updateChecking`/`updateApplying` (lines 301-323) and import the composable, wiring in RepoYeti's own `api.checkUpdate`/`api.applyUpdate`; replace `AppHeader.vue`'s click handler (lines 93-116) with the shared toast-branch helper, passing RepoYeti's own `t()`. Keep `web/src/types.ts`'s `UpdateStatus`/`UpdateApplyResult` DTOs as-is (per-app, with `service: "repoyeti"`).
- **Done when:** `web/src/store/index.ts` imports `useSelfUpdate` instead of defining the state machine locally; `AppHeader.vue` calls the shared toast-branch helper instead of inlining the if/else chain; `cd web && bun run test` and `cd web && bun run build` pass.

### G12. zod major version drift between RepoYeti (v4) and DevWebUI (v3); typescript drift in server tier too — effort: M
- **Files:** `package.json`, `D:/PublicProjects/DevWebUI/devwebui/package.json`
- **Problem:** RepoYeti's root `package.json` pins `"zod": "^4.4.3"` while DevWebUI's root `package.json` pins `"zod": "^3.25.76"` — a major-version difference (zod v4 has breaking API changes from v3). Server-side typescript also differs: RepoYeti pins `"typescript": "^5.7.2"` while DevWebUI pins `"typescript": "^6.0.3"` (matching the web-tier drift in G13). Reimagine has no zod or typescript dependency at all (untyped plain JS), so it's not part of this specific drift.
- **Fix:** Align on zod v4 (`^4.4.3`, the newer major already adopted by RepoYeti) in DevWebUI's `devwebui/package.json` and server code — this requires reviewing DevWebUI's `server/src/*.ts` + `shared/schema.ts` for any zod v3-only API usage before bumping. Align typescript to `^6.0.3` (the newer version, already used by DevWebUI) in RepoYeti's root `package.json`, then run `bun run typecheck` in RepoYeti to catch any TS 6 breakage.
- **Repos involved:** RepoYeti, DevWebUI.
- **This repo's part:** Bump root `package.json`'s `typescript` devDependency from `^5.7.2` to `^6.0.3`, then run `bun run typecheck` and fix any breakage surfaced by the newer compiler. (RepoYeti's `zod` is already the newer v4 — no change needed there; DevWebUI is the one that needs to bump zod.)
- **Done when:** `package.json` has `"typescript": "^6.0.3"`; `bun run typecheck` passes with the new compiler version.

### G13. Shared frontend stack (vue, vite, tailwindcss, typescript, vue-tsc, @lucide/vue) has drifted to different versions across all three web apps — effort: M
- **Files:** `web/package.json`, `D:/PublicProjects/DevWebUI/devwebui/web/package.json`, `D:/NEWProjects/active/Reimagine/src/web/package.json`, `D:/PublicProjects/lunarwerx-ui/package.json`
- **Problem:** vue is `^3.5.13` (RepoYeti) / `^3.5.39` (DevWebUI) / `^3.5.25` (Reimagine); tailwindcss is `^4.3.1` / `^4.3.2` / `^4.2.1`; typescript is `^5.7.2` / `^6.0.3` / `~5.9.3`; vite is `^6.0.5` / `^8.1.0` / `^7.3.1`; vue-tsc is `^2.2.0` / `^3.3.5` / `^3.1.5`; `@lucide/vue` is `^1.21.0` / `^1.22.0` / `^1.21.0`. reka-ui is aligned at `^2.10.1` in all three (not drifted). The kit's own `package.json` only pins vue `^3.5`, reka-ui `^2.10.1`, tailwindcss `^4.2` — no canonical version exists yet for vite/typescript/vue-tsc/@lucide/vue.
- **Fix:** Pick the newest already-in-use version for each: vue `^3.5.39`, tailwindcss `^4.3.2`, typescript `^6.0.3`, vite `^8.1.0`, vue-tsc `^3.3.5`, `@lucide/vue` `^1.22.0` (all currently used by DevWebUI, the newest across the trio). Update `web/package.json` and Reimagine's `src/web/package.json` to match, then run each app's install + typecheck to confirm no breakage (vite 6→8 and vue-tsc 2→3 are the biggest jumps and most likely to need config tweaks). Also add explicit version pins for vite/typescript/vue-tsc/@lucide/vue to `lunarwerx-ui/package.json` so future drift is caught by the kit's own dependency list.
- **Repos involved:** RepoYeti, DevWebUI, Reimagine.
- **This repo's part:** Bump `web/package.json`: vue to `^3.5.39`, tailwindcss to `^4.3.2`, typescript to `^6.0.3`, vite to `^8.1.0`, vue-tsc to `^3.3.5`, `@lucide/vue` to `^1.22.0`. Reinstall, then run `cd web && bun run build` and `cd web && bun run test` to confirm no breakage from the vite 6→8 and vue-tsc 2→3 jumps; fix any config/type fallout before considering this done.
- **Done when:** `web/package.json` pins match the versions above; `cd web && bun run build` and `cd web && bun run test` both pass.

### G14. RepoYeti's built OIDC/PKCE auth module is the only implementation of the Connections settings-sync identity layer — DevWebUI and Reimagine have none — effort: L
- **Files:** `src/auth.ts`, `CONNECTIONS_SETTINGS_SYNC.md`
- **Problem:** `src/auth.ts` already implements generic OIDC discovery (`discover()`, `jwks()`), PKCE (`pkce()`, `txs` transaction map with `gcTx()`), session signing (`sign()`/`unsign()`/`setSession()`/`readSession()`), and the full login/callback/logout handlers (`handleLogin`, `handleComplete`, `handleLogout`, `handleLogoutAll`) against a generic `OAuthConfig` (not RepoYeti-specific — `ownerMatches()` genericizes the owner-identity check). DevWebUI and Reimagine have zero occurrences of oidc/OIDC/connections.icu/oauth in their server source. Per `CONNECTIONS_SETTINGS_SYNC.md` §4c, a local daemon app should run the OIDC login itself and act as its own BFF — which is exactly the shape RepoYeti's `auth.ts` already has.
- **Fix:** Extract `auth.ts` into a shared, generic OIDC/PKCE module usable by DevWebUI and Reimagine. Correction from verification: `handleLogin`/`handleComplete` currently accept `cfg: RepoYetiConfig` and internally pull `cfg.oauth as OAuthConfig` — the extraction must change their signature to accept `OAuthConfig` (plus a session-secret/cookie-name parameter) directly rather than the full app config, so RepoYeti's own call sites need a thin adapter passing `cfg.oauth`/`cfg` fields through instead of the whole config object.
- **Repos involved:** RepoYeti, DevWebUI, Reimagine.
- **This repo's part:** Re-parameterize `src/auth.ts`'s `handleLogin`/`handleComplete` (and related handlers) to accept a bare `OAuthConfig` plus session-secret/cookie-name params instead of the full `RepoYetiConfig`, then add a thin adapter at RepoYeti's own call sites that extracts `cfg.oauth`/relevant `cfg` fields and passes them through. This re-parameterized module becomes the shared piece DevWebUI/Reimagine adopt.
- **Done when:** `src/auth.ts`'s `handleLogin`/`handleComplete` signatures take `OAuthConfig` (not `RepoYetiConfig`); RepoYeti's call sites pass `cfg.oauth` through an adapter; `bun test tests/` and `bun run typecheck` pass.

### G15. i18n bootstrap: Reimagine has none; RepoYeti's is a stripped-down copy of DevWebUI's more complete setup — effort: L
- **Files:** `web/src/i18n.ts`, `D:/PublicProjects/DevWebUI/devwebui/web/src/i18n/index.ts`, `D:/PublicProjects/DevWebUI/devwebui/web/src/i18n/locales/index.ts`, `D:/NEWProjects/active/Reimagine/src/web/src/main.ts`, `D:/NEWProjects/active/Reimagine/src/web/src/components/app/AppActionsBar.vue`
- **Problem:** RepoYeti's `i18n.ts` creates `vue-i18n` with `legacy:false, globalInjection:true, locale:"en", fallbackLocale:"en", messages:{en}` and exports a plain `t = i18n.global.t` — no persistence, no `<html lang>` sync, no locale registry. DevWebUI's `i18n/index.ts` additionally persists the chosen locale to `localStorage` (`initialLocale()`/`setLocale()`), maintains a `LocaleCode`/`LocaleMeta` registry with translation-provenance tracking, and syncs `document.documentElement.setAttribute("lang", ...)`. Reimagine has no vue-i18n dependency at all and hardcodes English strings directly in toast calls, inconsistent with RepoYeti/DevWebUI's `t("header.updateNone")`-style calls for the same self-update feature (see G11/G9).
- **Fix:** Promote DevWebUI's `i18n/index.ts` + `locales/index.ts` as canonical (it's a strict superset of RepoYeti's version). Add `lunarwerx-ui/src/lib/i18n-core.ts` exporting a factory `createAppI18n(messages: { en: object }, storageKey: string)` encapsulating DevWebUI's `initialLocale()`/`setLocale()`/registry-sync logic parameterized by app-specific storage key and messages, returning `{ i18n, setLocale, t }`. Add `lib/i18n-core.ts` to `sync.mjs`'s `SHARED` array. Reimagine needs the larger lift (add `vue-i18n` dependency, extract hardcoded strings, wire `createAppI18n` into `main.ts`) — that part is Reimagine's own burden.
- **Repos involved:** RepoYeti, DevWebUI, Reimagine.
- **This repo's part:** Once `lib/i18n-core.ts` is synced in, replace `web/src/i18n.ts`'s body with a 3-line call: `export const { i18n, t } = createAppI18n({ en }, "repoyeti.locale")`, gaining persistence, `<html lang>` sync, and the locale registry for free.
- **Done when:** `web/src/i18n.ts` calls the shared `createAppI18n` factory instead of constructing `createI18n` locally; `cd web && bun run test` and `cd web && bun run build` pass; manual check that the persisted locale and `<html lang>` attribute update correctly.

### G16. Theme/dark-mode: three different implementations (documenting concrete drift for the known pending unification pass) — effort: L
- **Files:** `web/src/lib/theme.ts`, `web/src/App.vue` (lines 8-11), `D:/PublicProjects/DevWebUI/devwebui/web/src/App.vue` (lines 8-11), `D:/NEWProjects/active/Reimagine/src/web/src/composables/useTheme.ts`, `D:/NEWProjects/active/Reimagine/src/web/src/App.vue` (line 18)
- **Problem:** This is the known pending theme-unification item; concrete drift observed: (1) RepoYeti (`lib/theme.ts`) wraps `@vueuse/core`'s `useColorMode({ initialValue: "dark", ... })` and adds custom cross-fade transition handling via a `theme-transitioning` CSS class with a 280ms timer and an `activeScopes` ref-count — genuinely more sophisticated than the other two. Defaults to forced dark. (2) DevWebUI's `App.vue` calls `useColorMode()` directly inline with no options object — defaults to OS preference (`auto`), no custom transition handling, no dedicated `theme.ts` file at all. (3) Reimagine's `composables/useTheme.ts` hand-rolls an equivalent using `useStorage` + `usePreferredDark` with its own `applyTheme()` that toggles the `dark` class, sets `html.dataset.theme`, sets `colorScheme`, AND additionally sets a `<meta name="theme-color">` tag — a capability neither RepoYeti nor DevWebUI have. Defaults to forced dark, like RepoYeti but unlike DevWebUI.
- **Fix:** This is the known pending unification pass — do not action immediately. When it happens: promote a merge of RepoYeti's transition-scoping logic + Reimagine's `theme-color` meta sync as the canonical `lunarwerx-ui/src/lib/useTheme.ts` (both are real, non-overlapping capabilities worth keeping; DevWebUI's bare `useColorMode()` call has nothing unique to preserve). Decide and document one default (dark, matching RepoYeti/Reimagine — DevWebUI's OS-auto default is the outlier). Add `lib/useTheme.ts` to `sync.mjs`'s `SHARED` array once written.
- **Repos involved:** RepoYeti, DevWebUI, Reimagine.
- **This repo's part:** Do not action yet (explicitly deferred pending the unification pass). When that pass happens, RepoYeti's `lib/theme.ts` transition-scoping logic (`activeScopes` ref-count, `theme-transitioning` class, 280ms timer) is the piece to contribute into the shared `useTheme.ts`; RepoYeti's `App.vue` then switches from its local `theme.ts` to the shared composable.
- **Done when:** Not yet actionable — defer until the theme-unification pass is scheduled. (No verify commands to run for RepoYeti until that pass begins.)

23 items: 1 design, 2 bespoke→kit, 2 dead code, 2 config/docs drift, 16 cross-repo.

---

### G17. [DONE 2026-07-03] Shared MCP stdio engine — all three apps' MCP servers now use ONE kit-synced engine [CROSS-REPO]
- **Canonical:** `D:/PublicProjects/lunarwerx-ui/src/server-lib/mcp-stdio.mjs` (+ `mcp-stdio.d.mts`) — a zero-dependency, runtime-agnostic (Bun + Node) JSON-RPC 2.0 / MCP dispatch + newline-delimited stdio loop. Synced by `sync.mjs`'s new `SERVER_LIB` list into each app's `serverLib` dir (`kit.config.json`); a new `--server-lib` flag syncs only these without touching the web layer.
- **Adopters:** RepoYeti (`src/mcp/core.ts` + `stdio.ts`), Reimagine (`src/mcp.js`), DevWebUI (`server/src/mcp.ts`, which DROPPED its `@modelcontextprotocol/sdk` dependency). Each app keeps only its own tool table (thin proxies to its own HTTP API) + `serverInfo`.
- **This repo's part (done):** `src/mcp/core.ts` and `src/mcp/stdio.ts` are now ~30-line adapters — they bind RepoYeti's dual backends (stdio → `httpBackend`, `POST /api/mcp` → `serviceBackend`) into the catalog and delegate to the engine, keeping the exact `handleRpc(msg, backend)` / `processLine` / `parseErrorResponse` API the route + tests use. The dispatch/error-code/stdin-loop logic that used to live here was deleted (it's the engine now).
- **Verified:** `bun test tests/mcp.test.ts` (12/12) + a real piped `repoyeti mcp` round-trip (initialize / tools-list=14 / -32700 parse error / silent notification) + `bun run typecheck` + the full `bun test tests/` suite (354 pass).
